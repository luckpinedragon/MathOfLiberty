<h1>Pages#filter</h1>

  <% @topics.each do |topic| %>
    <li>
      <div>
        <%= link_to topic.title, topics_show_path(id: topic.id) %>
      </div>
    </li>
  <% end %>
  
  <%= paginate @topics %>
    
